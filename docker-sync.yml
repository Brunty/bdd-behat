version: "2"
options:
    verbose: true
syncs:
    bdd_app-unison-sync:
        src: '.'
        sync_strategy: 'native_osx'
        sync_userid: '1000'
    bdd_db-unison-sync:
        src: './var/lib/mysql'
        sync_strategy: 'native_osx'
