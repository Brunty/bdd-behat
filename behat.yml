default:
    suites:
        domain:
            filters:
                tags: "@domain"
            contexts:
                - Contexts\Domain\BasketDomainContext
    autoload:
        '': '%paths.base%/features/bootstrap'